import math
def standarddev(arr,n):
    avg=sum(arr)/len(arr)
    c=0
    for a in arr:
        b=(a-avg)**2
        c+=b
    sd=math.sqrt(c/n)
    return sd
arr=[3,8,4,2,5,6,7]
n=len(arr)
print(standarddev(arr,n))
